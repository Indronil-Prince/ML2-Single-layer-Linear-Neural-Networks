Instructions to run the program:
-------------------------------------
1. Ensure you have Python installed on your machine.
2. Install required libraries by running 'pip install numpy pandas matplotlib'.
3. Keep the data files in folders "DryBeansDataset" for Dry beans data and "IrisDataset" for Iris data in same directory of the script.
4. Run the Python script to execute the program, "python single_models.py" for Perceptron, Adaline and SGD models.
   Run the Python script to execute the program, "python ovrsgd_model.py" for OvR-SGD model.
5. During providing user inputs, input as the program asks.